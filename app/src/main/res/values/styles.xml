<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="AlertDialogCustom" parent="Theme.AppCompat.Dialog.Alert">
        <!-- Background color -->
        <item name="android:background">#000000</item>
        <!-- Title and Message color -->
        <item name="android:textColor">#FFFFFF</item>
        <!-- Button colors -->
        <item name="android:buttonBarPositiveButtonStyle">@style/PositiveButtonTextStyle</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/NegativeButtonTextStyle</item>
        <item name="android:buttonBarButtonStyle">@style/CustomButtonStyle</item>
    </style>

    <style name="CustomButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:background">#000000</item>
    </style>

    <style name="PositiveButtonTextStyle" parent="TextAppearance.AppCompat.Button">
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="NegativeButtonTextStyle" parent="TextAppearance.AppCompat.Button">
        <item name="android:textColor">#FFFFFF</item>
    </style>



</resources>
